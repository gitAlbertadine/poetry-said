# poetry
```
#poetry self update
#poetry new package
#poetry new project --name package
poetry new --src package
cd package
#-existed project
#poetry init
poetry add django
#poetry add -D pytest
poetry show --tree
poetry shell


```
